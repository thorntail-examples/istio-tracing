swarm:
  jaeger:
    service-name: 'greeting'
    sampler-type: 'const'
    sampler-parameter: 1
    enable-b3-header-propagation: true
    reporter-log-spans: true
    remote-reporter-http-endpoint: 'http://jaeger-collector.istio-system.svc:14268/api/traces'
